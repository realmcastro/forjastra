# docs/auditorias — relatórios de segurança e performance

Donos: agents `seguranca` e `performance` (read-only sobre o resto do repo).
Arquivo: `AAAA-MM-DD-<escopo>.md`. Formato do achado nas regras de cada papel.

Achado ainda aberto que reaparece em auditoria posterior é marcado **reincidente** — a contagem de
reincidência é o sinal de que o processo, não o código, está falhando.
